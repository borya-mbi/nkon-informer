# 📥 Встановлення Git на Windows

## Швидкий спосіб: Git for Windows (Рекомендовано)

### Крок 1: Завантаження

1. Відкрийте: https://git-scm.com/download/win
2. Завантажиться автоматично **64-bit Git for Windows Setup**
3. Або натисніть **"Click here to download"**

### Крок 2: Встановлення

Запустіть інсталятор та використовуйте **рекомендовані налаштування**:

1. **Select Components:**
   - ☑ Windows Explorer integration (рекомендовано)
   - ☑ Git Bash Here
   - ☑ Git GUI Here
   - ☑ Associate .git* configuration files

2. **Choosing the default editor:**
   - Оберіть **Nano** (простий) або **Notepad++** (якщо встановлено)
   - Або залиште за замовчуванням (Vim - складніший)

3. **Adjusting your PATH environment:**
   - ☑ **Git from the command line and also from 3rd-party software** (рекомендовано)

4. **Choosing HTTPS transport backend:**
   - ☑ **Use the OpenSSL library** (рекомендовано)

5. **Configuring the line ending conversions:**
   - ☑ **Checkout Windows-style, commit Unix-style line endings** (рекомендовано для Windows)

6. **Configuring the terminal emulator:**
   - ☑ **Use MinTTY** (рекомендовано)

7. **Решта налаштувань:**
   - Залиште за замовчуванням

8. Натисніть **Install**

### Крок 3: Перевірка встановлення

Відкрийте **PowerShell** (або Git Bash):

```powershell
# Перевірка версії
git --version

# Має показати щось на зразок:
# git version 2.43.0.windows.1
```

### Крок 4: Початкова конфігурація

```powershell
# Налаштуйте ім'я та email (для GitHub commits)
git config --global user.name "borya-mbi"
git config --global user.email "your.email@example.com"

# Перевірка
git config --list
```

**Готово! Git встановлено!** ✅

---

## Альтернативний спосіб: GitHub Desktop (Графічний інтерфейс)

Якщо вам зручніше з графічним інтерфейсом:

### Варіант 1: GitHub Desktop

1. Завантажте: https://desktop.github.com/
2. Встановіть
3. Увійдіть в акаунт GitHub
4. Відкрийте папку проєкту

**Переваги:**
- ✅ Зручний UI
- ✅ Вбудована авторизація GitHub
- ✅ Візуалізація змін

**Недоліки:**
- ❌ Менш гнучкий ніж CLI
- ❌ Додатковий софт

### Варіант 2: Використання winget (Windows Package Manager)

Якщо у вас Windows 10/11:

```powershell
# Встановлення Git через winget
winget install --id Git.Git -e --source winget

# Перезапустіть PowerShell після встановлення
git --version
```

---

## Швидкий старт після встановлення

```powershell
# 1. Перейдіть у папку проєкту
cd h:\1c8\Work\MBI\Education\AG\nkon-informer

# 2. Ініціалізуйте Git
git init

# 3. Налаштуйте користувача (якщо не зробили раніше)
git config user.name "borya-mbi"
git config user.email "your@email.com"

# 4. Перевірте статус
git status

# 5. Готово! Тепер можете використовувати інструкції з GITHUB_QUICKSTART.md
```

---

## Корисні посилання

- 📘 Офіційна документація: https://git-scm.com/doc
- 🎓 Git tutorial: https://git-scm.com/docs/gittutorial
- 📺 Git for beginners (відео): https://www.youtube.com/watch?v=8JJ101D3knE

---

**Після встановлення Git переходьте до GITHUB_QUICKSTART.md для публікації проєкту!**
